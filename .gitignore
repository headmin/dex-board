node_modules/
dist/
.wrangler/
.dev.vars
.env
.alt-env
.alt*.env
*.log
.DS_Store
.venv/
docs/site/
src/dashboard/components.zip

# Local-only telemetry exports served by the Vite dev middleware (src/dev/).
# Real host names live in here — never commit.
local-data/
